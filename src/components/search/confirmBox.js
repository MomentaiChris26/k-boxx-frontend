import React from 'react';
import {Alert} from 'react-bootstrap'

const confirmBox=()=> {
    return (
        <Alert variant="success" className="confirmBox">
            Song added!
        </Alert>
    )
}
export default confirmBox